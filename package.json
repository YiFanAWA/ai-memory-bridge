{
  "name": "obsidian-ai-memory-bridge",
  "version": "0.4.1",
  "description": "Connect Obsidian notes as AI memory for Claude Code and other AI agents via MCP protocol. Drag & drop notes into a memory panel to give your AI agents a clear, organized knowledge base.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": ["obsidian", "plugin", "ai", "memory", "mcp", "claude"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "builtin-modules": "^4.0.0",
    "esbuild": "^0.20.0",
    "obsidian": "latest",
    "tslib": "^2.6.0",
    "typescript": "^5.3.0"
  }
}
